"""Phase 8: the parts of her that are not the model — affect and proactivity.

Both packages import `sidecar.core` and `sidecar.memory`, never the other
way around; nothing outside this package needs to know either exists.
"""
